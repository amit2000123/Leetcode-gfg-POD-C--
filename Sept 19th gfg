class Solution
{
    public:
    //Function to find position of first set bit in the given number.
    unsigned int getFirstSetBit(int n)
    {
      int b =-n;
      int c = n&b;
      if(c==0)
         return 0;
      return log2(c)+1;
    }
};
